const payments = {
    state: {
        
    },

    mutations: {
        setPlayer (state, player) {
            // state.users[user.id] = user - old
            // First filter by userId to see if player exists, if not, push onto players array?
        }
    }
}

export default payments
